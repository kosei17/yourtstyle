<h1>あなたのアイテム</h1>
<%= link_to "新規登録", new_item_path %>
<ul>
  <% @items.each do |i| %>
    <li>
      <% if i.image.attached? %>
        <%= image_tag i.image.variant(resize_to_limit: [80,80]) %>
      <% end %>
      <%= i.name %> (¥<%= i.price %>)
    </li>
  <% end %>
</ul>
